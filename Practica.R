# Mi primera practica con R
x <- "Hola"
y <- "UNTRM"
# f(x,y)=x + x^2 + 2xy
# ayuda:F1 Fn + F1


paste(x, y)

paste(x,y, sep =" + ")

paste(x, y)



library(tidyverse)

#pipe

a1 <- "estudiante"


a_1 <- "objet"]
a.1 <- "b"


notas <- c(16, 17, 19, 15)
media <- mean(notas)
media <- 
  
# ctrl +shift + m = %>%
m <- c(16, 17, 19, 15) %>% 
  mean()
m
  

x <- "Hola"
y <- "UNTRM"

paste(x, y)  


saludos<- funcion(aquien) 
x <- "Hola"
y <- "quien"
paste(x,y)


saludo2( ="fica")
saludo2(quien ="juan")

# -------------------------------------------------------------------

saludo3 <- function(texto, quien) 


x <- "texto"
y <- quien

  paste(x, y) 


saludo3(texto = "como:estas", quien =. "FICA") 
#.......................................

# funcjon para calcular la nota de la unidad 1
# PA = 30%, AC = 20%, Y EX = 50
#Crear una funcion donde me da la media ponderada

nota <- function(PA, AC, EX) 
peso_PA <- 0.30  # 30%
peso_AC <- 0.20  # 20%
peso_EX <- 0.50  # 50%

nota_final <- (PA * peso_PA) + (AC * peso_AC) + (EX * peso_EX)

PA <- 80  # Nota de PA (por ejemplo, 80%)
AC <- 70  # Nota de AC (por ejemplo, 70%)
EX <- 90  # Nota de EX (por ejemplo, 90%)




# crear una funcion para determinar el area 
# de una circunferencia
# el usuario debe seleccionar si es en cm o mm




